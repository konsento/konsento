$('.modal-title').html("<%= Comment.model_name.human(count: 2) %>")
$('.modal-body').html('');
$('.modal-body').append("<div class='modal-comments'></div>");

<% @proposal.comments.each do |c| %>
  $('.modal-comments').append("<%= escape_javascript(render partial: 'comments/comment', locals: {comment: c}) %>");
<% end %>

$('.modal-body').append("<%= escape_javascript(render partial: 'comments/form', locals: {commentable: @comment}) %>");
$('#modal').modal('show');
$('#modal').modal('handleUpdate');

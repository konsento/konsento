$('.modal-title').html("<%= t '.title' %>")
$('.modal-body').html('');

$('.modal-body').append(
  "<%= escape_javascript(render partial: 'proposals/propose', locals: {proposal: @proposal, new_proposal: @new_proposal}) %>"
);

$('#modal').modal('show');
$('#modal').modal('handleUpdate');

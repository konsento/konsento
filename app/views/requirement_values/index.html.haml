.row
  .panel.panel-default.col-md-6.col-md-offset-3
    .panel-body
      %h2
        .text-center
          = RequirementValue.model_name.human(count: 2)
      - @requirement_values.each do |r|
        %b
          = r.join_requirement.title
        %p
          = r.value

.row
  .col-lg-12
    .panel.panel-default
      .panel-heading
        %h3.panel-title Registre-se
      .panel-body
        %p= "Você foi convidado por #{@invitation.user.username}"
        = simple_form_for @user, url: register_invitations_path(token: params[:token]) do |f|
          = f.input :email, disabled: true
          = f.input :username
          = f.input :password
          = f.input :password_confirmation
          = f.button :submit
